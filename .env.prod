# backend service base url, prod environment (same-origin when served by platform-api)
VITE_SERVICE_BASE_URL=/api/console/v1

# other backend service base url, prod environment
VITE_OTHER_SERVICE_BASE_URL= `{
  "demo": ""
}`
